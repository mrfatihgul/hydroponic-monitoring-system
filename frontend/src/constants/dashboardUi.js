export const MAIN_CHART_HEIGHT_PX = 280;

export const MAX_SYSTEM_LOG_ENTRIES = 120;
export const NFT_CHANNEL_COUNT = 6;

export const EVENT_LOG_MAX_HEIGHT_CLASS = "max-h-56";

/** Max width for the critical monitoring column (narrow, left-aligned). */
export const CRITICAL_MONITOR_MAX_CLASS = "max-w-sm";

export const TREND_EPSILON = {
  ph: 0.01,
  ec: 0.02,
  sicaklik: 0.06,
  nem: 0.25,
  suSeviyesi: 0.35,
};
